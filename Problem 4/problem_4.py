# IDEA: Have an upper bound and a lowe bound, and update it with every item in the array.
# At the end, if the lower bound isn't in the array, that's the first integer missing. 
# If it is in the array, the upper bound is the integer missing instead. 